import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sbuzzrum.personalfinance',
  appName: 'personal-finance',
  webDir: 'dist'
};

export default config;
